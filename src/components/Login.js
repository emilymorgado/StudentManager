import React from 'react';
import { Nav, Header, Input, Button } from './common';

const Login = () => {
  return (
    <div>
      <Nav />
      <Header />
      <Input />
      <Input />
      <Button />
    </div>
  );
}

export default Login;
